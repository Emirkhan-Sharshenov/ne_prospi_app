<?xml version="1.0" encoding="utf-8"?>
<!-- Пункт проверки: значок в кружке, название, пояснение, «OK» или «Исправить». -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/SurfaceCard"
    android:layout_marginBottom="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="16dp">

        <FrameLayout
            android:id="@+id/checkIconBg"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/circle"
            android:backgroundTint="@color/surface_mid">

            <ImageView
                android:id="@+id/checkIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:importantForAccessibility="no" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/checkTitle"
                style="@style/Text.Title"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/checkDesc"
                style="@style/Text.Body.Muted"
                android:textSize="14sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/checkOk"
            style="@style/StatusPill"
            android:layout_marginStart="8dp"
            android:background="@drawable/pill"
            android:backgroundTint="@color/accent_container"
            android:text="@string/ok"
            android:textColor="@color/on_accent_container"
            app:drawableStartCompat="@drawable/ic_check_circle"
            app:drawableTint="@color/on_accent_container" />

        <Button
            android:id="@+id/checkFix"
            style="@style/Button.Pill"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:minHeight="48dp"
            android:paddingHorizontal="18dp"
            android:text="@string/fix" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
